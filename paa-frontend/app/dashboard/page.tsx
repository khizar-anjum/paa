'use client';

// This page component is required for Next.js App Router to recognize the /dashboard route
// The actual rendering is handled by the layout.tsx file which shows the PersistentChatPanel
// when pathname === '/dashboard'

export default function DashboardPage() {
  // The layout handles all the rendering for the dashboard route
  // This component exists only to satisfy Next.js App Router requirements
  return null;
}